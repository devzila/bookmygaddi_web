<div class="container">
  <table class="table">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th></th>
    </tr>
    <%@leads.each do |lead| %>
    <tr>
      <td><%=lead.name%></td>
      <td><%=lead.email%></td>
    </tr>
    <%end%>
  </table>
</div>
