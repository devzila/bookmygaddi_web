<div class="container">
<button class="btn"><%= link_to "New Forms", new_admin_form_path%></td></button></a>
<table class="table">
    <tr>
      <th>Slug</th>
      <th>Content</th>
    </tr>
    <%@forms.each do |form| %>
    <tr>
      <td><%=link_to form.slug, admin_form_path(form)%></td>
      <td><%=form.content%></td>
      <td><%=link_to "Edit", edit_admin_form_path(form)%></td>
      <td><%=link_to "Delete", admin_form_path(form), method: :delete%></td>
    </tr>
    <%end%>
  </table>
</div>